import { InfiniteObsScrollDirective } from './infinite-obs-scroll.directive';

describe('InfiniteObsScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiniteObsScrollDirective();
    expect(directive).toBeTruthy();
  });
});
